name: SomnaLux
options:
  bundleIdPrefix: com.Alex
  xcodeVersion: "16.0"
  deploymentTarget:
    iOS: "17.0"
settings:
  PRODUCT_BUNDLE_IDENTIFIER: com.Alex.SomnaLux
  INFOPLIST_FILE: ios/SomnaLux/Info.plist
  GENERATE_INFOPLIST_FILE: false
  DEVELOPMENT_TEAM: "523MJL63MC"
  CODE_SIGN_STYLE: Automatic
  SWIFT_VERSION: "5.9"
  CURRENT_PROJECT_VERSION: "${BUILD_NUMBER:-18}"
  MARKETING_VERSION: "1.0"
  ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
  ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
  ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: false
targets:
  SomnaLux:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: ios/SomnaLux
    settings:
      INFOPLIST_FILE: ios/SomnaLux/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.Alex.SomnaLux
      DEVELOPMENT_TEAM: "523MJL63MC"
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      CODE_SIGN_ENTITLEMENTS: ios/SomnaLux/SomnaLux.entitlements
